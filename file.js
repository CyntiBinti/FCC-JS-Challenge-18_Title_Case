function titleCase(str) {
  for (let i = 0; i < str.length; i++) {
    let regex = //; //this finds the right letter
    let replacement = //; //this states what to replace with
    let capitalised = str.replace(regex, replacement);
  }
  return capitalised;
}

titleCase("I'm a little tea pot");
